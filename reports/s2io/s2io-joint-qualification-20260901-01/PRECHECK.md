# S2-IO Precheck

- Qualification ID: `s2io-joint-qualification-20260901-01`
- Scope: neutral S2-IN lifecycle, current 14 S2-ID checks, ParentSetV1 and private run shell
- Registered invocation: `python -m unittest tests.test_s2io_joint_qualification -v`
- Expected test bodies: `34`
- Allowed invocations: exactly one
- Retry: forbidden
- Real histories, formations and memory-state functions: excluded
- Main gate before execution: `False`
- Registry: `183` operations / `366` events
- Bootstrap limit: `11264` bytes
- Earliest active failure limit: `22528` bytes
- Untracked bootstrap caller: excluded

The qualification is technical only. It cannot establish a memory-function result.
